{
    "source_id": "hdfs-logs-kafka-source",
    "source_type": "kafka",
    "params": {
        "topic": "cloudera-cluster-logs",
        "client_params": {
            "bootstrap.servers": "localhost:9092"
        }
    }
}
