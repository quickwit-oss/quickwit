{
    "source_id": "hdfs-logs-kafka-source",
    "num_pipelines": 2,
    "source_type": "kafka",
    "params": {
        "topic": "cloudera-cluster-logs",
        "client_params": {
            "bootstrap.servers": "host:9092"
        }
    }
}
