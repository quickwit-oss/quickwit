{
    "store_source": true,
    "default_search_fields": ["body", "title"],
    "field_mappings": [
        {
            "name": "body",
            "type": "text",
            "tokenizer": "en_stem",
            "record": "position"
        },
        {
            "name": "title",
            "type": "text",
            "tokenizer": "en_stem",
            "record": "position"
        },
        {
            "name": "url",
            "type": "text",
            "tokenizer": "raw"
        }
    ]
}
