id: gcp.gce.route
metric_id: google-compute-engine
backend_only: false
facets:
  - groups:
      - Event
    name: Event Name
    path: evt.name
    source: log
  - groups:
      - Event
    name: Event Outcome
    path: evt.outcome
    source: log
  - groups:
      - Web Access
    name: User-Agent
    path: http.useragent
    source: log
  - groups:
      - Web Access
    name: Browser
    path: http.useragent_details.browser.family
    source: log
  - groups:
      - Web Access
    name: Device
    path: http.useragent_details.device.family
    source: log
  - groups:
      - Web Access
    name: OS
    path: http.useragent_details.os.family
    source: log
  - groups:
      - Geoip
    name: City Name
    path: network.client.geoip.city.name
    source: log
  - groups:
      - Geoip
    name: Continent Code
    path: network.client.geoip.continent.code
    source: log
  - groups:
      - Geoip
    name: Continent Name
    path: network.client.geoip.continent.name
    source: log
  - groups:
      - Geoip
    name: Country ISO Code
    path: network.client.geoip.country.iso_code
    source: log
  - groups:
      - Geoip
    name: Country Name
    path: network.client.geoip.country.name
    source: log
  - groups:
      - Geoip
    name: Subdivision ISO Code
    path: network.client.geoip.subdivision.iso_code
    source: log
  - groups:
      - Geoip
    name: Subdivision Name
    path: network.client.geoip.subdivision.name
    source: log
  - groups:
      - Web Access
    name: Client IP
    path: network.client.ip
    source: log
  - groups:
      - User
    name: User Email
    path: usr.email
    source: log
  - groups:
      - User
    name: User ID
    path: usr.id
    source: log
  - groups:
      - Geoip
    name: AS Domain
    path: network.client.geoip.as.domain
    source: log
  - groups:
      - GCP
    name: Project ID
    path: project_id
    source: tag
pipeline:
  type: pipeline
  name: Google Cloud Routes
  enabled: true
  filter:
    query: source:gcp.gce.route
  processors:

    - type: date-remapper
      name: Define `data.timestamp`, `timestamp` as the official date of the log
      enabled: true
      sources:
        - data.timestamp
        - timestamp
    - type: status-remapper
      name: Define `data.severity`, `severity` as the official status of the log
      enabled: true
      sources:
        - data.severity
        - severity
    - type: service-remapper
      name: Define `data.protoPayload.serviceName`, `protoPayload.serviceName` as the official service of the log
      enabled: true
      sources:
        - data.protoPayload.serviceName
        - protoPayload.serviceName
    - type: message-remapper
      name: Define `data.textPayload`, `textPayload`, `data.jsonPayload.MESSAGE`, `jsonPayload.MESSAGE` as the official message of the log
      enabled: true
      sources:
        - data.textPayload
        - textPayload
        - data.jsonPayload.MESSAGE
        - jsonPayload.MESSAGE
    - type: attribute-remapper
      name: Map `data.protoPayload.requestMetadata.callerSuppliedUserAgent`, `protoPayload.requestMetadata.callerSuppliedUserAgent` to `http.useragent`
      enabled: true
      sources:
        - data.protoPayload.requestMetadata.callerSuppliedUserAgent
        - protoPayload.requestMetadata.callerSuppliedUserAgent
      target: http.useragent
      preserveSource: false
      overrideOnConflict: false
      sourceType: attribute
      targetType: attribute
    - type: user-agent-parser
      name: ''
      enabled: true
      sources:
        - http.useragent
      target: http.useragent_details
      encoded: false
    - type: attribute-remapper
      name: Map `data.protoPayload.requestMetadata.callerIp`, `protoPayload.requestMetadata.callerIp` to `network.client.ip`
      enabled: true
      sources:
        - data.protoPayload.requestMetadata.callerIp
        - protoPayload.requestMetadata.callerIp
      target: network.client.ip
      preserveSource: false
      overrideOnConflict: false
      sourceType: attribute
      targetType: attribute
    - type: geo-ip-parser
      name: Fetching GeoIp data for client ip
      enabled: true
      sources:
        - network.client.ip
      target: network.client.geoip
    - type: attribute-remapper
      name: Map `data.protoPayload.authenticationInfo.principalEmail`, `protoPayload.authenticationInfo.principalEmail` to `usr.id`
      enabled: true
      sources:
        - data.protoPayload.authenticationInfo.principalEmail
        - protoPayload.authenticationInfo.principalEmail
      target: usr.id
      preserveSource: true
      overrideOnConflict: false
      sourceType: attribute
      targetType: attribute
    - type: attribute-remapper
      name: Map `data.protoPayload.authenticationInfo.principalEmail`, `protoPayload.authenticationInfo.principalEmail` to `usr.email`
      enabled: true
      sources:
        - data.protoPayload.authenticationInfo.principalEmail
        - protoPayload.authenticationInfo.principalEmail
      target: usr.email
      preserveSource: true
      overrideOnConflict: false
      sourceType: attribute
      targetType: attribute
    - type: attribute-remapper
      name: Map `data.protoPayload.methodName`, `protoPayload.methodName` to `evt.name`
      enabled: true
      sources:
        - data.protoPayload.methodName
        - protoPayload.methodName
      target: evt.name
      preserveSource: false
      overrideOnConflict: false
      sourceType: attribute
      targetType: attribute
    - type: attribute-remapper
      name: Map `data.protoPayload.status.message`, `protoPayload.status.message` to `evt.outcome`
      enabled: true
      sources:
        - data.protoPayload.status.message
        - protoPayload.status.message
      target: evt.outcome
      targetFormat: string
      preserveSource: false
      overrideOnConflict: false
      sourceType: attribute
      targetType: attribute
