---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [quickwit/Cargo.toml, quickwit/quickwit-metrics-engine/Cargo.toml, quickwit/quickwit-metrics-engine/src/lib.rs, quickwit/quickwit-metrics-engine/src/schema/mod.rs, quickwit/quickwit-metrics-engine/src/query/mod.rs, quickwit/quickwit-metrics-engine/src/split/mod.rs]
autonomous: true
---

<objective>
Establish DataFusion/Parquet infrastructure and create the quickwit-metrics-engine crate.

Purpose: Lay the foundation for the metrics engine by adding required dependencies and creating the crate structure that subsequent plans will build upon.
Output: New `quickwit-metrics-engine` crate with module placeholders, DataFusion and Parquet dependencies available in workspace.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md

Existing crate patterns to follow:
@quickwit/quickwit-opentelemetry/Cargo.toml
@quickwit/quickwit-opentelemetry/src/lib.rs

Existing Arrow dependency in workspace:
@quickwit/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DataFusion and Parquet dependencies to workspace</name>
  <files>quickwit/Cargo.toml</files>
  <action>
Add DataFusion and Parquet as workspace dependencies in the [workspace.dependencies] section.

Add these entries (match existing Arrow version 54):
```toml
datafusion = { version = "44", default-features = false }
parquet = { version = "54", default-features = false, features = ["arrow", "zstd", "snappy"] }
```

Why these versions:
- DataFusion 44 is current stable, compatible with Arrow 54
- Parquet 54 matches Arrow version for compatibility
- Features: arrow (required), zstd/snappy (compression options)

Do NOT add object_store - the existing quickwit-storage abstraction will be used.
  </action>
  <verify>cd quickwit && cargo check --workspace 2>&1 | head -20 (should resolve deps without errors)</verify>
  <done>DataFusion 44 and Parquet 54 added to workspace dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Create quickwit-metrics-engine crate structure</name>
  <files>quickwit/quickwit-metrics-engine/Cargo.toml, quickwit/quickwit-metrics-engine/src/lib.rs, quickwit/quickwit-metrics-engine/src/schema/mod.rs, quickwit/quickwit-metrics-engine/src/query/mod.rs, quickwit/quickwit-metrics-engine/src/split/mod.rs</files>
  <action>
Create new crate `quickwit-metrics-engine` in the workspace.

1. Add to workspace members in quickwit/Cargo.toml:
   ```toml
   members = [
       # ... existing members
       "quickwit-metrics-engine",
   ]
   ```

2. Create quickwit/quickwit-metrics-engine/Cargo.toml:
   ```toml
   [package]
   name = "quickwit-metrics-engine"
   version.workspace = true
   edition.workspace = true
   license.workspace = true

   [dependencies]
   arrow = { workspace = true }
   datafusion = { workspace = true }
   parquet = { workspace = true }
   serde = { workspace = true }
   thiserror = { workspace = true }
   tracing = { workspace = true }

   [dev-dependencies]
   proptest = { workspace = true }
   ```

3. Create lib.rs with module structure:
   ```rust
   //! Pomsky Metrics Engine
   //!
   //! High-throughput metrics storage and query engine using DataFusion/Parquet.
   //! Replaces Tantivy for metrics workloads while maintaining full compatibility
   //! with Tantivy for logs/traces.

   pub mod schema;
   pub mod query;
   pub mod split;
   ```

4. Create placeholder modules with doc comments:
   - src/schema/mod.rs: "Metrics schema definitions for Parquet storage"
   - src/query/mod.rs: "DataFusion query execution infrastructure"
   - src/split/mod.rs: "Metrics split format replacing Tantivy segments"

Follow existing crate patterns from quickwit-opentelemetry for Cargo.toml structure.
  </action>
  <verify>cd quickwit && cargo check -p quickwit-metrics-engine</verify>
  <done>quickwit-metrics-engine crate compiles with module structure in place</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd quickwit && cargo check --workspace` succeeds
- [ ] `cargo check -p quickwit-metrics-engine` succeeds
- [ ] datafusion and parquet appear in Cargo.lock
- [ ] Module structure exists: schema/, query/, split/
</verification>

<success_criteria>
- All tasks completed
- New crate quickwit-metrics-engine exists and compiles
- DataFusion and Parquet dependencies available in workspace
- Module placeholders ready for subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
