---
phase: 09-testing-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quickwit/quickwit-integration-tests/src/test_utils/cluster_sandbox.rs
  - quickwit/quickwit-integration-tests/Cargo.toml
autonomous: true
---

<objective>
Add OTLP metrics client infrastructure to integration test framework.

Purpose: Enable end-to-end testing of the Pomsky metrics pipeline by adding MetricsServiceClient to ClusterSandbox, following the same pattern as logs_client() and trace_client().
Output: ClusterSandbox with metrics_client() method ready for use in metrics integration tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-search-integration/08-02-SUMMARY.md

# Integration test infrastructure
@quickwit/quickwit-integration-tests/src/test_utils/cluster_sandbox.rs
@quickwit/quickwit-integration-tests/src/tests/otlp_tests.rs

# OTLP metrics proto types
@quickwit/quickwit-proto/src/codegen/opentelemetry/opentelemetry.proto.collector.metrics.v1.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add OTLP metrics client import to cluster_sandbox</name>
  <files>quickwit/quickwit-integration-tests/src/test_utils/cluster_sandbox.rs</files>
  <action>
    Add import for MetricsServiceClient from quickwit_proto:
    ```rust
    use quickwit_proto::opentelemetry::proto::collector::metrics::v1::metrics_service_client::MetricsServiceClient;
    ```
    This follows the same pattern as LogsServiceClient and TraceServiceClient imports already in the file.
  </action>
  <verify>cargo check -p quickwit-integration-tests compiles without errors</verify>
  <done>MetricsServiceClient imported in cluster_sandbox.rs</done>
</task>

<task type="auto">
  <name>Task 2: Add metrics_client() method to ClusterSandbox</name>
  <files>quickwit/quickwit-integration-tests/src/test_utils/cluster_sandbox.rs</files>
  <action>
    Add metrics_client() method to ClusterSandbox impl, following the pattern of trace_client():
    ```rust
    pub fn metrics_client(&self) -> MetricsServiceClient<tonic::transport::Channel> {
        MetricsServiceClient::new(self.channel(QuickwitService::Indexer))
    }
    ```
    Place it immediately after trace_client() method (around line 382).
  </action>
  <verify>cargo check -p quickwit-integration-tests compiles without errors</verify>
  <done>metrics_client() method available on ClusterSandbox</done>
</task>

<task type="auto">
  <name>Task 3: Verify test infrastructure compiles</name>
  <files>quickwit/quickwit-integration-tests/Cargo.toml</files>
  <action>
    Run full compile of integration tests crate to verify no missing dependencies:
    ```bash
    cargo build -p quickwit-integration-tests
    ```
    If MetricsServiceClient requires additional features, add them to Cargo.toml.
    The quickwit-proto dependency should already have the necessary OpenTelemetry types.
  </action>
  <verify>cargo build -p quickwit-integration-tests succeeds</verify>
  <done>Integration test crate builds successfully with metrics client</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p quickwit-integration-tests` succeeds
- [ ] `cargo build -p quickwit-integration-tests` succeeds
- [ ] metrics_client() method exists on ClusterSandbox
- [ ] No new warnings introduced
</verification>

<success_criteria>

- All tasks completed
- MetricsServiceClient properly imported
- metrics_client() method available for use in tests
- Follows existing code patterns (logs_client, trace_client)
</success_criteria>

<output>
After completion, create `.planning/phases/09-testing-validation/09-01-SUMMARY.md`
</output>
