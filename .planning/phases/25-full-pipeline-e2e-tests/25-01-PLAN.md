---
phase: 25-full-pipeline-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quickwit/quickwit-indexing/tests/metrics_infra_e2e_test.rs
  - quickwit/Makefile
autonomous: true
---

<objective>
Implement full pipeline E2E tests against real Minio S3 storage and Postgres metastore.

Purpose: Validate the complete metrics pipeline (Ingest → WAL → Parquet → metastore → query) works end-to-end with real infrastructure, providing confidence to ship.
Output: E2E test functions in `metrics_infra_e2e_test.rs` that exercise the full pipeline against Docker Compose services.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-e2e-test-infrastructure/24-01-SUMMARY.md
@.planning/phases/23-docker-compose-setup/23-01-SUMMARY.md

# Test infrastructure already in place:
@quickwit/quickwit-indexing/tests/metrics_infra_e2e_test.rs

# Existing mock-based E2E tests (reference for patterns):
@quickwit/quickwit-indexing/src/actors/metrics_e2e_test.rs

# Pipeline actors:
@quickwit/quickwit-indexing/src/actors/metrics_doc_processor.rs
@quickwit/quickwit-indexing/src/actors/metrics_indexer.rs
@quickwit/quickwit-indexing/src/actors/metrics_uploader.rs
@quickwit/quickwit-indexing/src/actors/metrics_publisher.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add full pipeline E2E test with real infrastructure</name>
  <files>quickwit/quickwit-indexing/tests/metrics_infra_e2e_test.rs</files>
  <action>
Add a new test function `test_full_metrics_pipeline_e2e` that:

1. Uses `check_infra_available()` to skip gracefully if Docker Compose not running
2. Creates `TestInfra::new()` to get real Minio storage and Postgres metastore
3. Sets up a test index using `setup_test_index()`
4. Creates the full pipeline: MetricsDocProcessor → MetricsIndexer → MetricsUploader → MetricsPublisher
   - Use the REAL storage from TestInfra (S3CompatibleObjectStorage pointing to Minio)
   - Use the REAL metastore from TestInfra (PostgresqlMetastore)
5. Ingests test data (100-200 rows with varied metric names, services, timestamps)
6. Sends through pipeline with force_commit to trigger split production
7. Verifies:
   - Data flows through all pipeline stages (check actor counters)
   - Split is staged in metastore (list staged splits)
   - Split is published in metastore (list published splits)
   - Parquet file exists in Minio storage
8. Cleans up test index using `cleanup_test_index()`

Mark test with `#[ignore]` to run only via explicit Makefile target.

Use existing test helper functions from `metrics_e2e_test.rs`:
- `create_dict_array()`, `create_nullable_dict_array()`, `create_variant_array()`
- `create_test_batch()` or `create_varied_test_batch()`
- `create_raw_doc_batch()`, `record_batch_to_ipc()`

Import necessary types:
- From quickwit_actors: Universe, ActorHandle
- From quickwit_metastore: ListMetricsSplitsQuery, ListMetricsSplitsRequestExt, ListMetricsSplitsResponseExt
- From quickwit_proto::metastore: MetastoreService, ListMetricsSplitsRequest
- From quickwit_storage: Storage

The test should print clear output showing each stage passing, similar to `test_infra_connectivity()`.
  </action>
  <verify>
cargo test --package quickwit-indexing --test metrics_infra_e2e_test --features testsuite,postgres -- test_full_metrics_pipeline_e2e --ignored --nocapture

Test should print stage-by-stage progress and pass with Docker Compose running.
  </verify>
  <done>
Test function exists, compiles, and passes when Docker Compose infrastructure is running. Output shows data flowing through all pipeline stages with real storage and metastore.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add query accuracy E2E test with real infrastructure</name>
  <files>quickwit/quickwit-indexing/tests/metrics_infra_e2e_test.rs</files>
  <action>
Add a new test function `test_query_accuracy_e2e` that:

1. Uses `check_infra_available()` to skip gracefully
2. Creates `TestInfra::new()`
3. Sets up a test index
4. Ingests test data with KNOWN values:
   - 100 rows of metric "test.e2e.cpu" with values 0.0..99.0
   - 100 rows of metric "test.e2e.memory" with values 100.0..199.0
   - Timestamps from 10000..10199
   - Service tags: "web-server", "api-server" (50/50 split)
5. Runs data through full pipeline to S3 and metastore
6. Lists published splits from metastore using ListMetricsSplitsRequest
7. Downloads Parquet file(s) from Minio storage
8. Queries via DataFusion and verifies:
   - COUNT(*) = 200
   - COUNT(*) WHERE metric_name = 'test.e2e.cpu' = 100
   - MIN(value) = 0.0, MAX(value) = 199.0
   - AVG(value) for cpu metric = 49.5
   - Time range filter: COUNT(*) WHERE timestamp_secs >= 10100 = 100
   - Tag filter: COUNT(*) WHERE tag_service = 'web-server' = 100 (approximately)
9. Cleans up

Mark test with `#[ignore]`.

Use DataFusion SessionContext and MetricsTableProvider from quickwit_metrics_engine::query.
Download parquet files from storage using `storage.get_all()` to temp directory before querying.

The test should print each query with expected vs actual values.
  </action>
  <verify>
cargo test --package quickwit-indexing --test metrics_infra_e2e_test --features testsuite,postgres -- test_query_accuracy_e2e --ignored --nocapture

Test should print each query result and pass with Docker Compose running.
  </verify>
  <done>
Test function exists, compiles, and passes. Query results match expected values within tolerance.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add test-metrics-e2e Makefile target</name>
  <files>quickwit/Makefile</files>
  <action>
Add a new Makefile target `test-metrics-e2e` that runs all E2E tests against the Docker Compose infrastructure:

```makefile
test-metrics-e2e:
	@echo "Running metrics E2E tests against Docker Compose infrastructure..."
	@echo "Ensure infrastructure is running: make docker-metrics-up"
	cargo test --package quickwit-indexing --test metrics_infra_e2e_test --features testsuite,postgres -- --ignored --nocapture
```

Place it near the existing `test-metrics-infra` target for discoverability.

The target should:
- Print helpful message about ensuring infrastructure is running
- Run all ignored tests in the metrics_infra_e2e_test file
- Include --nocapture so test output is visible
  </action>
  <verify>
make test-metrics-e2e
(with docker-metrics-up already running)

All E2E tests should execute and pass.
  </verify>
  <done>
Makefile target exists and successfully runs all E2E tests.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo build --package quickwit-indexing --tests --features testsuite,postgres` succeeds
- [ ] With Docker Compose running (`make docker-metrics-up`):
  - [ ] `make test-metrics-infra` passes (connectivity)
  - [ ] `make test-metrics-e2e` passes (full pipeline + query accuracy)
- [ ] Tests gracefully skip when infrastructure not available
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- E2E tests provide confidence that full pipeline works end-to-end with real infrastructure
- Tests are documented and easy to run via Makefile
</success_criteria>

<output>
After completion, create `.planning/phases/25-full-pipeline-e2e-tests/25-01-SUMMARY.md`
</output>
