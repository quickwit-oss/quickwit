---
phase: 10-production-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quickwit/quickwit-metrics-engine/Cargo.toml
  - quickwit/quickwit-metrics-engine/src/lib.rs
  - quickwit/quickwit-metrics-engine/src/metrics.rs
autonomous: true
---

<objective>
Add Prometheus metrics instrumentation to the metrics engine for operational observability.

Purpose: Enable monitoring of metrics engine performance, throughput, and error rates in production.
Output: Prometheus metrics module with counters, histograms, and gauges following quickwit-common patterns.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@quickwit/quickwit-common/src/metrics.rs
@quickwit/quickwit-metrics-engine/src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add prometheus dependency and create metrics module</name>
  <files>quickwit/quickwit-metrics-engine/Cargo.toml, quickwit/quickwit-metrics-engine/src/metrics.rs, quickwit/quickwit-metrics-engine/src/lib.rs</files>
  <action>
    1. Add `quickwit-common` workspace dependency to Cargo.toml for access to metrics utilities
    2. Create `src/metrics.rs` with Prometheus metrics following quickwit-common patterns:
       - `METRICS_INGEST_BATCHES_TOTAL` (IntCounter) - Total batches processed
       - `METRICS_INGEST_ROWS_TOTAL` (IntCounter) - Total rows ingested
       - `METRICS_INGEST_BYTES_TOTAL` (IntCounter) - Total bytes ingested
       - `METRICS_INGEST_DURATION_SECONDS` (Histogram) - Batch processing duration
       - `METRICS_SPLITS_WRITTEN_TOTAL` (IntCounter) - Total splits written
       - `METRICS_SPLITS_BYTES_WRITTEN` (IntCounter) - Total bytes written to splits
       - `METRICS_QUERY_DURATION_SECONDS` (Histogram) - Query execution duration
       - `METRICS_QUERY_ROWS_RETURNED` (IntCounter) - Total rows returned from queries
       - `METRICS_ERRORS_TOTAL` (IntCounterVec<1>) - Errors by operation type ["ingest", "query", "storage"]
    3. Use `quickwit_common::metrics::{new_counter, new_histogram, IntCounterVec}` utilities
    4. Export module from lib.rs with `pub mod metrics;`
  </action>
  <verify>cargo check -p quickwit-metrics-engine</verify>
  <done>Metrics module compiles with all counters and histograms defined</done>
</task>

<task type="auto">
  <name>Task 2: Instrument ingest pipeline with metrics</name>
  <files>quickwit/quickwit-metrics-engine/src/ingest/accumulator.rs, quickwit/quickwit-metrics-engine/src/ingest/processor.rs</files>
  <action>
    1. In `accumulator.rs`:
       - Import metrics from `crate::metrics`
       - In `add_batch()`: Increment METRICS_INGEST_BATCHES_TOTAL, METRICS_INGEST_ROWS_TOTAL (batch.num_rows())
       - In `flush()`: Increment METRICS_SPLITS_WRITTEN_TOTAL when split is produced
       - Observe METRICS_INGEST_DURATION_SECONDS for batch processing time
    2. In `processor.rs`:
       - Import metrics from `crate::metrics`
       - In `process_ipc()`: Increment METRICS_INGEST_BYTES_TOTAL (ipc_bytes.len())
       - On error: Increment METRICS_ERRORS_TOTAL with label ["ingest"]
  </action>
  <verify>cargo check -p quickwit-metrics-engine</verify>
  <done>Ingest operations emit Prometheus metrics</done>
</task>

<task type="auto">
  <name>Task 3: Instrument query execution with metrics</name>
  <files>quickwit/quickwit-metrics-engine/src/query/context.rs</files>
  <action>
    1. Import metrics from `crate::metrics`
    2. In `MetricsSessionContext::sql()` (or execution point):
       - Start timer for METRICS_QUERY_DURATION_SECONDS
       - Observe duration after query completes
       - Increment METRICS_QUERY_ROWS_RETURNED based on result row count
       - On error: Increment METRICS_ERRORS_TOTAL with label ["query"]
  </action>
  <verify>cargo check -p quickwit-metrics-engine && cargo test -p quickwit-metrics-engine</verify>
  <done>Query operations emit Prometheus metrics, all tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p quickwit-metrics-engine` succeeds
- [ ] `cargo test -p quickwit-metrics-engine` passes all 109+ tests
- [ ] Metrics module exports all defined counters and histograms
- [ ] Ingest and query paths instrument metrics
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Prometheus metrics defined and instrumented for ingest and query paths
- No new warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/10-production-hardening/10-01-SUMMARY.md`
</output>
