---
phase: 10-production-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quickwit/quickwit-metrics-engine/Cargo.toml
  - quickwit/quickwit-metrics-engine/src/ingest/processor.rs
  - quickwit/quickwit-metrics-engine/src/ingest/accumulator.rs
  - quickwit/quickwit-metrics-engine/src/storage/writer.rs
  - quickwit/quickwit-metrics-engine/src/storage/split_writer.rs
autonomous: true
---

<objective>
Add structured tracing/logging to the metrics engine for debugging and operational visibility.

Purpose: Enable operators to trace requests through the metrics pipeline and diagnose issues.
Output: Consistent tracing instrumentation across ingest and storage modules using tracing crate.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@quickwit/quickwit-metrics-engine/src/ingest/processor.rs
@quickwit/quickwit-metrics-engine/src/storage/writer.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tracing dependency and instrument ingest</name>
  <files>quickwit/quickwit-metrics-engine/Cargo.toml, quickwit/quickwit-metrics-engine/src/ingest/processor.rs, quickwit/quickwit-metrics-engine/src/ingest/accumulator.rs</files>
  <action>
    1. Add `tracing = { workspace = true }` to Cargo.toml dependencies
    2. In `processor.rs`:
       - Add `use tracing::{debug, warn, instrument};`
       - Add `#[instrument(skip(self, ipc_bytes), fields(bytes_len = ipc_bytes.len()))]` to `process_ipc()`
       - Add debug! for successful IPC decode with row count
       - Add warn! for schema mismatch errors with expected vs actual counts
    3. In `accumulator.rs`:
       - Add `use tracing::{debug, info, warn};`
       - Add debug! in `add_batch()` showing batch size and accumulated rows
       - Add info! in `flush()` when producing a split with row count and byte size
       - Add warn! for threshold exceeded conditions
  </action>
  <verify>cargo check -p quickwit-metrics-engine</verify>
  <done>Ingest modules have structured tracing instrumentation</done>
</task>

<task type="auto">
  <name>Task 2: Instrument storage layer with tracing</name>
  <files>quickwit/quickwit-metrics-engine/src/storage/writer.rs, quickwit/quickwit-metrics-engine/src/storage/split_writer.rs</files>
  <action>
    1. In `writer.rs`:
       - Add `use tracing::{debug, instrument};`
       - Add `#[instrument(skip(self, batch), fields(batch_rows = batch.num_rows()))]` to `write_to_bytes()`
       - Add `#[instrument(skip(self, batch), fields(batch_rows = batch.num_rows(), path = %path.display()))]` to `write_to_file()`
       - Add debug! for write completion with bytes written
    2. In `split_writer.rs`:
       - Add `use tracing::{debug, info, instrument};`
       - Add `#[instrument(skip(self, batch), fields(batch_rows = batch.num_rows()))]` to `write_split()`
       - Add debug! for time range extraction
       - Add info! when split file written with split_id, file path, byte size
  </action>
  <verify>cargo check -p quickwit-metrics-engine && cargo test -p quickwit-metrics-engine</verify>
  <done>Storage modules have structured tracing instrumentation, all tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p quickwit-metrics-engine` succeeds
- [ ] `cargo test -p quickwit-metrics-engine` passes all tests
- [ ] Ingest and storage paths have tracing instrumentation
- [ ] No new warnings introduced
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Tracing instrumentation added to ingest and storage paths
- Debug and info level logs provide operational visibility
  </success_criteria>

<output>
After completion, create `.planning/phases/10-production-hardening/10-02-SUMMARY.md`
</output>
