---
phase: 10-production-hardening
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - quickwit/quickwit-metrics-engine/src/ingest/processor.rs
  - quickwit/quickwit-metrics-engine/src/ingest/accumulator.rs
  - quickwit/quickwit-metrics-engine/src/storage/writer.rs
  - quickwit/quickwit-metrics-engine/src/storage/split_writer.rs
  - quickwit/quickwit-metrics-engine/src/query/context.rs
  - quickwit/quickwit-metrics-engine/src/query/aggregation.rs
autonomous: true
---

<objective>
Replace unwrap() calls with proper error handling in production code paths.

Purpose: Prevent runtime panics from malformed data or edge cases, returning proper errors instead.
Output: All production code paths use Result-based error handling with descriptive error variants.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@quickwit/quickwit-metrics-engine/src/ingest/processor.rs
@quickwit/quickwit-metrics-engine/src/storage/writer.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix error handling in ingest modules</name>
  <files>quickwit/quickwit-metrics-engine/src/ingest/processor.rs, quickwit/quickwit-metrics-engine/src/ingest/accumulator.rs</files>
  <action>
    1. In `processor.rs`:
       - Line 120: `batches.into_iter().next().unwrap()` → Already inside if-check, but add ok_or for safety: `.ok_or(IngestError::UnexpectedBatchCount(0))?`
    2. In `accumulator.rs`:
       - Review all unwrap() calls - most are in #[cfg(test)] blocks which are acceptable
       - Any production unwrap() calls should be converted to use `?` with appropriate error type
       - Note: If all unwrap() are in test code, document this as "no changes needed"
  </action>
  <verify>cargo check -p quickwit-metrics-engine</verify>
  <done>Ingest modules have no unwrap() in production code paths</done>
</task>

<task type="auto">
  <name>Task 2: Fix error handling in storage modules</name>
  <files>quickwit/quickwit-metrics-engine/src/storage/writer.rs, quickwit/quickwit-metrics-engine/src/storage/split_writer.rs</files>
  <action>
    1. In `writer.rs`:
       - Lines 126, 137, 144: Replace `.unwrap()` on RecordBatch creation with proper error propagation
       - These are in test helper functions but exposed publicly - wrap in Result or use expect() with descriptive message
       - If RecordBatch::try_new can fail, propagate the error
    2. In `split_writer.rs`:
       - Review unwrap() calls - most are likely in #[cfg(test)] blocks
       - Any production unwrap() should be replaced with `?` operator
       - For min/max extraction, return Option or error on empty batches
  </action>
  <verify>cargo check -p quickwit-metrics-engine</verify>
  <done>Storage modules have proper error handling in production paths</done>
</task>

<task type="auto">
  <name>Task 3: Fix error handling in query modules</name>
  <files>quickwit/quickwit-metrics-engine/src/query/context.rs, quickwit/quickwit-metrics-engine/src/query/aggregation.rs</files>
  <action>
    1. In `context.rs`:
       - Most unwrap() are in #[cfg(test)] - verify and document
       - Any production unwrap() should use `?` with MetricsQueryError
    2. In `aggregation.rs`:
       - Line 240: `self.time_bucket.as_ref().unwrap()` - this is inside a conditional, but add safeguard or validate at construction time
       - Either ensure time_bucket is always Some when group_by_parts needs it, or return an error
    3. Run full test suite to ensure changes don't break anything
  </action>
  <verify>cargo check -p quickwit-metrics-engine && cargo test -p quickwit-metrics-engine</verify>
  <done>Query modules have proper error handling, all 109+ tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p quickwit-metrics-engine` succeeds with no warnings about unwrap
- [ ] `cargo test -p quickwit-metrics-engine` passes all tests
- [ ] Production code paths (non-test) have no unwrap() calls
- [ ] Error types provide descriptive messages
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No unwrap() in production code paths
- Proper Result-based error propagation throughout
  </success_criteria>

<output>
After completion, create `.planning/phases/10-production-hardening/10-03-SUMMARY.md`
</output>
