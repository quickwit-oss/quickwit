---
phase: 10-production-hardening
plan: 04
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - quickwit/quickwit-search/src/metrics_leaf.rs
  - quickwit/quickwit-indexing/src/actors/metrics_doc_processor.rs
  - quickwit/quickwit-indexing/src/actors/metrics_indexer.rs
autonomous: true
---

<objective>
Add observability to the integration layer connecting metrics engine to quickwit pipeline.

Purpose: Enable monitoring of metrics pipeline performance at the integration boundary.
Output: Tracing and metrics instrumentation in doc processor, indexer, and search leaf modules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@quickwit/quickwit-search/src/metrics_leaf.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance metrics_leaf.rs observability</name>
  <files>quickwit/quickwit-search/src/metrics_leaf.rs</files>
  <action>
    1. Add structured tracing fields to existing debug! calls:
       - In `leaf_search_metrics_split`: Add fields for storage_uri, parquet_path
       - Add debug! after SQL execution showing query plan summary
    2. Add error-level logging for failure paths:
       - warn! when split load fails with split_id and error details
       - warn! when DataFusion query fails with SQL and error
    3. Add metrics counters (import from quickwit-common):
       - `METRICS_SEARCH_QUERIES_TOTAL` - queries executed
       - `METRICS_SEARCH_ERRORS_TOTAL` - query errors by type
       - Observe query duration in existing cpu_time tracking
  </action>
  <verify>cargo check -p quickwit-search</verify>
  <done>metrics_leaf.rs has comprehensive tracing and metrics</done>
</task>

<task type="auto">
  <name>Task 2: Add observability to metrics doc processor</name>
  <files>quickwit/quickwit-indexing/src/actors/metrics_doc_processor.rs</files>
  <action>
    1. Add tracing instrumentation:
       - Add `use tracing::{debug, info, warn, instrument};`
       - Add `#[instrument(skip(self, doc_batch), fields(batch_len))]` to process method
       - Add debug! showing batch format detection (Arrow IPC vs other)
       - Add info! when routing batch to metrics pipeline with batch size
    2. Add error logging:
       - warn! for invalid format detection with batch details
       - warn! for processing failures with error context
  </action>
  <verify>cargo check -p quickwit-indexing</verify>
  <done>MetricsDocProcessor has tracing instrumentation</done>
</task>

<task type="auto">
  <name>Task 3: Add observability to metrics indexer</name>
  <files>quickwit/quickwit-indexing/src/actors/metrics_indexer.rs</files>
  <action>
    1. Add tracing instrumentation:
       - Add `use tracing::{debug, info, warn};`
       - Add info! when split is produced with split_id, row_count, byte_size
       - Add debug! for batch accumulation progress
    2. Add metrics (import from quickwit-common):
       - Use existing metrics patterns from quickwit-indexing
       - Track splits produced, rows indexed
    3. Run tests to ensure changes don't break functionality
  </action>
  <verify>cargo check -p quickwit-indexing && cargo test -p quickwit-indexing --lib -- metrics</verify>
  <done>MetricsIndexer has tracing and metrics instrumentation, tests pass</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p quickwit-search` succeeds
- [ ] `cargo check -p quickwit-indexing` succeeds
- [ ] Integration layer has comprehensive tracing
- [ ] Metrics counters track pipeline throughput
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Integration layer fully instrumented for observability
- Operators can trace requests through metrics pipeline
  </success_criteria>

<output>
After completion, create `.planning/phases/10-production-hardening/10-04-SUMMARY.md`
</output>
