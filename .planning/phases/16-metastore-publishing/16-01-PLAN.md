---
phase: 16-metastore-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quickwit/quickwit-indexing/src/actors/metrics_publisher.rs
  - quickwit/quickwit-indexing/src/actors/metrics_uploader.rs
  - quickwit/quickwit-indexing/src/actors/indexing_pipeline.rs
  - quickwit/quickwit-indexing/src/actors/mod.rs
autonomous: true
---

<objective>
Complete the metrics split publishing lifecycle by creating MetricsPublisher actor and wiring it into the pipeline.

Purpose: After this phase, staged metrics splits become queryable. MetricsUploader sends MetricsSplitsUpdate to MetricsPublisher, which calls publish_metrics_splits on the metastore to transition splits from "Staged" to "Published" state.

Output: MetricsPublisher actor with full pipeline wiring and tests.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-metastore-staging/15-01-SUMMARY.md
@.planning/phases/15-metastore-staging/15-02-SUMMARY.md
@.planning/phases/16-metastore-publishing/16-CONTEXT.md

# Key source files to reference:
@quickwit/quickwit-indexing/src/actors/publisher.rs
@quickwit/quickwit-indexing/src/actors/metrics_uploader.rs
@quickwit/quickwit-indexing/src/actors/indexing_pipeline.rs
@quickwit/quickwit-indexing/src/models/metrics_splits_update.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MetricsPublisher actor</name>
  <files>quickwit/quickwit-indexing/src/actors/metrics_publisher.rs, quickwit/quickwit-indexing/src/actors/mod.rs</files>
  <action>
Create a new MetricsPublisher actor that mirrors the Publisher actor pattern for metrics splits:

1. Create `metrics_publisher.rs` with:
   - `MetricsPublisherCounters` struct with `num_published_splits: u64`, `num_replace_operations: u64`, `num_empty_splits: u64`
   - `MetricsPublisher` struct with fields:
     - `metastore: MetastoreServiceClient`
     - `counters: MetricsPublisherCounters`
   - Implement `Actor` trait with:
     - `ObservableState = MetricsPublisherCounters`
     - `queue_capacity = QueueCapacity::Bounded(1)` (match Publisher)
     - `name() = "MetricsPublisher"`
   - Implement `Handler<MetricsSplitsUpdate>`:
     - Extract `index_id`, `new_splits`, `replaced_split_ids`, `publish_lock` from message
     - Acquire publish_lock (if lock dead, return early)
     - Build `PublishMetricsSplitsRequest` with staged_split_ids from new_splits metadata
     - Call `metastore.publish_metrics_splits(request)` using `ctx.protect_future()`
     - Update counters appropriately

2. Add unit tests:
   - `test_metrics_publisher_publishes_splits` - verify metastore.publish_metrics_splits called with correct split IDs
   - `test_metrics_publisher_handles_empty_splits` - verify empty splits increment counter
   - `test_metrics_publisher_respects_publish_lock` - verify dead lock prevents publish

3. Export from `mod.rs`: `MetricsPublisher`, `MetricsPublisherCounters`

Use `quickwit_proto::metastore::PublishMetricsSplitsRequest` for the request type.
Do NOT add MergePlanner or source mailbox handling - those can be added later if needed for merge support.
  </action>
  <verify>cargo check -p quickwit-indexing && cargo test -p quickwit-indexing metrics_publisher</verify>
  <done>MetricsPublisher actor compiles, handles MetricsSplitsUpdate messages, and all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Update MetricsSplitsUpdateMailbox to send to MetricsPublisher</name>
  <files>quickwit/quickwit-indexing/src/actors/metrics_uploader.rs</files>
  <action>
Update the MetricsSplitsUpdateMailbox enum and its send() implementation:

1. Change the enum variant:
   - Keep `Publisher(Mailbox<MetricsPublisher>)` but update the type from `Mailbox<Publisher>` to `Mailbox<MetricsPublisher>`
   - Update the `From` impl accordingly

2. Update the `send()` method:
   - Remove the TODO comment and placeholder log
   - Actually send the `MetricsSplitsUpdate` to the `MetricsPublisher` mailbox using `ctx.send_message()`
   - Handle send errors appropriately (log and return error)

3. Update imports to use MetricsPublisher instead of Publisher

4. Update tests:
   - Change `universe.create_test_mailbox::<Publisher>()` to `universe.create_test_mailbox::<MetricsPublisher>()`
   - Verify the test still compiles and passes
  </action>
  <verify>cargo check -p quickwit-indexing && cargo test -p quickwit-indexing metrics_uploader</verify>
  <done>MetricsSplitsUpdateMailbox sends to MetricsPublisher, tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Wire MetricsPublisher in spawn_metrics_pipeline</name>
  <files>quickwit/quickwit-indexing/src/actors/indexing_pipeline.rs</files>
  <action>
Update spawn_metrics_pipeline to use MetricsPublisher instead of Publisher:

1. Import MetricsPublisher at top of file

2. In spawn_metrics_pipeline():
   - Replace Publisher creation with MetricsPublisher:
     ```rust
     let metrics_publisher = MetricsPublisher::new(
         self.params.metastore.clone(),
     );
     let (metrics_publisher_mailbox, metrics_publisher_handle) = ctx
         .spawn_actor()
         .set_kill_switch(self.kill_switch.clone())
         .spawn(metrics_publisher);
     ```
   - Update MetricsUploader creation to use metrics_publisher_mailbox:
     ```rust
     MetricsSplitsUpdateMailbox::Publisher(metrics_publisher_mailbox)
     ```
   - Remove the old Publisher spawn for metrics pipeline (it's not needed for metrics)

3. Update any logging to reflect MetricsPublisher being used

4. Keep the existing merge_planner_mailbox reference for future merge support, but don't wire it to MetricsPublisher yet (that's not in scope)
  </action>
  <verify>cargo check -p quickwit-indexing</verify>
  <done>spawn_metrics_pipeline uses MetricsPublisher, compiles successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo check -p quickwit-indexing` succeeds
- [ ] `cargo test -p quickwit-indexing` passes all tests
- [ ] MetricsPublisher actor exists with Handler<MetricsSplitsUpdate>
- [ ] MetricsSplitsUpdateMailbox sends to MetricsPublisher
- [ ] spawn_metrics_pipeline wires MetricsPublisher correctly
- [ ] No TypeScript/Rust errors
</verification>

<success_criteria>

- MetricsPublisher actor created with publish_metrics_splits integration
- MetricsUploader properly sends MetricsSplitsUpdate to MetricsPublisher
- Pipeline spawn wires all components correctly
- All tests pass
- Staged splits can now be published (completing the staging→published lifecycle)
</success_criteria>

<output>
After completion, create `.planning/phases/16-metastore-publishing/16-01-SUMMARY.md`
</output>
