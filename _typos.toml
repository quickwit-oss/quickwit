[files]
extend-exclude = [
    "**/*.json",
    # Vendored Datadog agent-payload protobuf definitions — keep as-is so
    # they diff cleanly against upstream github.com/DataDog/agent-payload.
    "quickwit/pomsky-dd-protos/proto/**",
]

[default.extend-words]
# Don't correct the surname "Teh"
strat = "strat"
